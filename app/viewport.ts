import type { Viewport } from 'next'

export const viewport: Viewport = {
  themeColor: '#dc2626', // Primary red color
  colorScheme: 'light dark',
  viewport: 'width=device-width, initial-scale=1',
  maximumScale: 1,
  userScalable: false,
}